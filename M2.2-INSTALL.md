# M2.2 Install
Copy this directory into the repository root, then run:
```bash
python -m unittest discover -s tests -v
python scripts/validate_report.py data/latest.json
git add -A
git commit -m "feat: add SharBo Globo agents and validation gate"
git push origin main
```
